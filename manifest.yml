applications:
  - name: pal-tracker
    path: build/libs/pal-tracker.jar
    routes:
      - route: krel.apps.evans.pal.pivotal.io
    env:
      MESSAGE: Hello from the KREL review environment.
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 11.+ } }'
